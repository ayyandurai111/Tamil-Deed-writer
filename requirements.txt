# Tamil Sale Deed MCP Server — Python dependencies
# Install: pip install -r requirements.txt

# MCP framework — 1.9.0+ required for Streamable HTTP (GPT support)
mcp[cli]>=1.9.0

# HTTP server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Word document generation (Tamil font support)
python-docx>=1.1.0

# Async + type support
anyio>=4.3.0
