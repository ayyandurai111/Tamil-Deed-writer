# Tamil Sale Deed MCP Server — Python dependencies
# Install: pip install -r requirements.txt

# MCP framework (includes SSE transport, FastAPI integration)
mcp[cli]>=1.5.0

# HTTP server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Word document generation (Tamil font support)
python-docx>=1.1.0

# Async + type support (pulled in by mcp, listed explicitly for clarity)
anyio>=4.3.0
