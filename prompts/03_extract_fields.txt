[CALL 3 of 12] [TOOL: extract_fields]
ONE TASK: call extract_fields tool only.

Before calling — YOU extract from user text:
  VENDOR_NAME / FATHER / AGE / ADDRESS / AADHAAR(12 digits) / PHONE / PREFIX
  PURCHASER_NAME / FATHER / AGE / ADDRESS / AADHAAR(12 digits) / PHONE / PREFIX
  DATE_DAY / DATE_MONTH(Tamil name) / DATE_YEAR
  TOTAL_AMOUNT(digits only) / NANJAI_OR_PUNJAI / other fields
  Not in text → null. Never guess.
  Loop call: extracted_fields = new reply only; existing_fields = full accumulated dict.

→ Next separate response: CALL 4 resolve_date
