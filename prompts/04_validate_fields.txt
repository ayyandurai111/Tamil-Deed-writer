[CALL 5 of 12] [TOOL: validate_fields]
ONE TASK: call validate_fields tool only.
Pass deed_type (CALL 1) and fields dict (extract_fields + resolve_date merged).

After tool returns:
  CASE A — can_generate=True, pan_block=False
    → Next separate response: CALL 6 fill_skeleton

  CASE B — can_generate=False  [NO TOOL — ask user]
    → Ask: "பத்திரம் உருவாக்க கீழ்கண்ட விவரங்கள் தேவை:
            1. [field]?  2. [field]? ..."
    → After reply: loop CALL 3 → CALL 4 → CALL 5

  CASE C — pan_block=True  [NO TOOL — ask user]
    → Ask: "விற்பனை தொகை ₹10 லட்சத்திற்கு மேல் — PAN கட்டாயம்:
            விற்பவர் PAN: ?  வாங்குபவர் PAN: ?"
    → Do NOT proceed to fill_skeleton while pan_block=True. Loop CALL 5.
