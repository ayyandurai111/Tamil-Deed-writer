[CALL 11 of 12] [TOOL: generate_docx]
ONE TASK: call generate_docx tool only.
PRECONDITION: CALL 10 decision must be CASE A or CASE B (confirmed) with ready_for_docx=True.
Do NOT call if ready_for_docx=False or if CALL 7 (tool) + CALL 8/9/10 (your analysis) are not yet complete.

Pass:
  filled_skeleton = CALL 6 "clean_skeleton"
  filename_prefix = "vendorname_purchasername" (e.g. "ramasamy_murugan")

After tool returns:
  success=True  → if PAN/TDS notes exist, show them. Show disclaimer. → Next: CALL 12
  success=False → "❌ தோல்வி: [error] — மீண்டும் முயற்சிக்கவும்."
