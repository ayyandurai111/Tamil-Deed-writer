[CALL 1 of 12] [TOOL: detect_deed_type]
ONE TASK: call detect_deed_type tool only.

Before calling: YOU read the user prompt and determine the deed type.
  Agriculture = விவசாய நிலம், ஏக்கர், நஞ்சை, புஞ்சை, survey no, பட்டா, FMB
  Plot        = மனை, site, sqft, door no, ward, layout, residential
  Context wins over keywords — if unclear, default to plot.

After tool returns: tell user "[deed_type] பத்திரம் தயாரிக்கிறோம்."
→ Next separate response: CALL 2 load_skeleton
