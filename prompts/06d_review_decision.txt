[CALL 10 of 12] [YOUR DECISION — NO TOOL CALL]
ONE TASK: make exactly one final decision using CALL 7 + CALL 8 + CALL 9 results combined.
Do NOT make a tool call in this same response.

DECISION RULE — ready_for_docx=True requires ALL of:
  • CALL 7 (review_draft tool) returned no critical L1/L2 errors
  • CALL 8 (your L3) found no blocking inconsistencies
  • CALL 9 (your L4) found no grammar errors

  CASE A — all three checks passed, no errors, no warnings:
    → "✅ சரிபார்ப்பு முடிந்தது — பத்திரம் உருவாக்குகிறேன்."
    → ready_for_docx = True → Next separate response: CALL 11 generate_docx

  CASE B — warnings only (minor, no blocking errors) in L3 or L4:
    → "⚠️ சிறு குறிப்புகள்:\n[list]\nதொடரட்டுமா? (ஆம்/இல்லை)"
    → ஆம் from user → ready_for_docx = True → CALL 11
    → இல்லை → ask for corrections → loop CALL 3

  CASE C — L1 or L2 critical errors (placeholder leak, boundary missing, date invalid):
    → "❌ திருத்தங்கள் தேவை:\n🔴 [list]\nதிருத்திய விவரங்கள் கொடுங்கள்."
    → ready_for_docx = False → loop CALL 6 fill_skeleton

  CASE D — L2 Aadhaar or PAN format invalid:
    → "❌ [field] தவறானது — சரியான எண் கொடுங்கள்."
    → ready_for_docx = False → loop CALL 5 validate_fields

NOTE: If CALL 7 ready_for_docx=False AND L3/L4 also have issues → use CASE C or D (most severe).
Only call generate_docx when ready_for_docx=True is confirmed here.
