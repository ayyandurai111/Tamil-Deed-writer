TOOL: resolve_date
STEP: CALL 4 of 12 — ALWAYS CALL
══════════════════════════════════════

ONE TASK: resolve_date tool call மட்டும். எப்போதும் call செய் — skip செய்யாதே.

என்ன செய்:
  பயனர் date கொடுத்தால்  → user_input = அந்த raw text
                              உதாரணம்: '15/05/2026', 'மே 15 2026', 'இன்று', 'நேற்று'
  பயனர் date இல்லாவிட்டால் → user_input = ''
                              tool தானாக இன்றைய தேதி பயன்படுத்தும்

tool call செய். முடிந்தது.

Return-ல்:
  source='today_default' → பயனருக்கு சொல்: "தேதி கொடுக்கவில்லை — இன்று [DATE_FULL] பயன்படுகிறது."
  source='user_provided'  → silent (சொல்ல வேண்டாம்)

⚠️ tool call மட்டும் — அடுத்த call: validate_fields (CALL 5).
