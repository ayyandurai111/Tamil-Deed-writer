[CALL 4 of 12] [TOOL: resolve_date] — ALWAYS call, never skip.
ONE TASK: call resolve_date tool only.

  User gave a date  → user_input = that raw text (e.g. "15/05/2026", "மே 15", "today")
  No date given     → user_input = ""  (tool uses today automatically)

After tool returns:
  source="today_default" → tell user: "தேதி கொடுக்கவில்லை — இன்று [DATE_FULL] பயன்படுகிறது."
  source="user_provided" → silent.
Merge returned date fields into existing fields dict.
→ Next separate response: CALL 5 validate_fields
