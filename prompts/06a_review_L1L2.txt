[CALL 7 of 12] [TOOL: review_draft]
ONE TASK: call review_draft tool only.
Pass clean_skeleton (CALL 6 "clean_skeleton") and deed_type (CALL 1 result).

This tool runs L1+L2 programmatic checks only.
  L1 = placeholder leak check (any {{TAG}} remaining)
  L2 = legal field checks (Aadhaar, PAN, date validity, boundaries)

Store the returned ready_for_docx value — needed for CALL 10 decision.
Tell user: "பத்திர சரிபார்ப்பு (L1+L2) துவங்குகிறது..."

→ Next separate response: CALL 8 — YOUR L3 analysis (no tool)
→ Then separate: CALL 9 — YOUR L4 analysis (no tool)
→ Then separate: CALL 10 — YOUR final decision (no tool)
